#include<stdlib.h>
#include<stdio.h>
#include<string.h>

typedef struct integerNode()
{

	int x;
	int *next;

}Int_node();

// linked lists + insert ->	careful
//							what you need

//search(find) ->	restaurant review
//					name
//					start at the head and traverse through while cheking the name field until you find the name
//					once found return a pointer to the correct review, or return a copy of the review
//					a pointer uses less memory, but allows user to change the contents
//					a copy uses more memory, however only allows user to see what it is rather than change what it is


int main()
{

}


//queue adt:
//			First in first out FIFO
//			